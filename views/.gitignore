# Node.js / npm stuff - NEVER upload these!
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment / Secrets - CRITICAL for security!
.env
.env.local
.env.development
.env.production

# Logs and temporary files
logs/
*.log
*.logs

# Build / dist folders (if you ever add frontend build)
dist/
build/

# Editor / OS files (common on Mac)
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# Your uploads folder - ignore contents but keep the empty folder if needed
public/uploads/*
!public/uploads/.gitkeep   # Optional: see Step 4

# MongoDB / database files (if you ever have local dumps)
*.dump
mongod.log

# Misc
Thumbs.db
ehthumbs.db
public/uploads/*
!public/uploads/.gitkeep